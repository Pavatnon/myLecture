---------------------------------------------------------------
JavaScript: Keyword
----------------------------------------------------------------
alert: แสดง popup
typeof: แสดงประเภทของ Datatype
prompt: แสดงหน้าต่าง input
-----------------------------------------------------------------
JavaScript: MathFunction
-----------------------------------------------------------------
Math.pow(base,up) --> ยกกำลัง --> math.pow(5,2) = 5^2
Math.round --> ปัดเศษ ตามลกษณะทั่วไป
Math.floor --> ปัดเศษลง
Math.random --> เป็นการ สุ่มตัวเลข ระหว่าง 0 - 0.9999
-----------------------------------------------------------------
JavaScript: Datatype
-----------------------------------------------------------------
sring: word-> อักขระม ตัวอักษร
number: just number -> ตัวเลข
bool: ture, false -> จริง, เท็จ

-----------------------------------------------------------------
JavaScript: Variables
-----------------------------------------------------------------
var เป็นการสร้างตัวแปร ที่ใช้ จัดเก็บ หรือ จดจำข้อมูลในรูปแบบต่างๆ syntax var variableName = value
        var มีรูปแบบคล้ายการสร้าง กล่องจัดเก็บข้อมูลที่สามารถจัดเก็บข้อมูลได้ ทุกประเภท และสามารถเปลี่ยนแปลงได้

-----------------------------------------------------------------
JavaScript: String 
-----------------------------------------------------------------
length --> นับจำนวน string 
slice --> ตัดส่วนที่เกินจากที่กำหนดออก variable.slice(x,y) --> x ตำแหน่งแรก y ตำแหน่งสุดท้าย
toUpperCase --> เปลี่ยน อักขระ ให้เป็นตัว พิมพ์ใหญ่
-----------------------------------------------------------------
JavaScript: Condition
-----------------------------------------------------------------
if - else --> if(condition){ behavior} else {behavior}
comparation --> ===, >, <, <=, >=

== --> เท่ากันหรือไม่ โดยไม่ตรวจสอบ datatype
=== --> เท่ากันหรือไม่ โดยตรวจสอบ datatype

Combining
&& --> And if (condition && condition) {behavior}
|| --> Or  if (condition || condition) {behavior}
! --> Not  if (!condition) {behavior}
-----------------------------------------------------------------
JavaScript: Operator
-----------------------------------------------------------------
+ - * / --> บวก ลบ คูณ หาร
% --> หาร หาเศษ
() --> ใช้เพื่อจัดลำดับความสำคัญ ในการ คำนวณ เช่น (3+5) * 2 = 16 จะคำนวณ 3+5 ก่อน *2
++: +1 
--: -1
+= -= *= /= : x = x+? 
-----------------------------------------------------------------
JavaScript: Function
-----------------------------------------------------------------
keyword: function FuntionName(parameter){ function behavior }

parameter --> สามารถมีมกกว่า1 ได้ โดยใช้ ", "  เช่น   function FuntionName(parameter1, parameter1){ function behavior }

return --> สามารถใช้เก็บค่าของ finction ได้ เช่น   function calAB(a, b){return a+b}--> ค่าที่  return เข้า function นี้ = a+b
-----------------------------------------------------------------
JavaScript: Array
-----------------------------------------------------------------
Syntax --> var name = [v1, v2]; position start at 0. 

.lenght --> จำนวนทั้งหมดที่อยู่ใร array
.includes --> ค้นหา obj ที่อยู่ใน array
.push --> เพิ่ม obj เข้าไปใน array
.pop --> ลบ obj ตัวสุดท้าย ออกจาก aray
-----------------------------------------------------------------
JavaScript: Loops
-----------------------------------------------------------------
While loops : while (somting is true){behavior}

for loops : for (i = 0; i = 2; i++){behavior}

